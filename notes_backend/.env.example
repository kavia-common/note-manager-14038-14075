# Environment variables example for notes_backend (copy to .env and fill values)
# Secret key for JWT token encoding
JWT_SECRET_KEY=your-super-secret-here

# Database connection string (default uses local SQLite notes.db)
DATABASE_URL=sqlite:///notes.db
